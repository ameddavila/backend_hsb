import { Sequelize } from "sequelize-typescript";

// Actualmente no hay relaciones estrictamente necesarias
export const associateConfigModels = (sequelize: Sequelize) => {
  // Si en el futuro agregas modelos como ConfigLog o AuditoriaConfig, puedes relacionarlos aquí.
};
